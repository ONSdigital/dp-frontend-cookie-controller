---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tags: 1.13.7

inputs:
  - name: dp-frontend-cookie-controller
    path: dp-frontend-cookie-controller

outputs:
  - name: build

caches:
  - path: go/

run:
  path: dp-frontend-cookie-controller/ci/scripts/build.sh
